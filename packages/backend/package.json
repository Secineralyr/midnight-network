{
  "name": "@midnight-network/backend",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "dependencies": {
    "@midnight-network/entity": "workspace:*",
    "@midnight-network/rank-calc": "workspace:*",
    "@midnight-network/shared": "workspace:*",
    "@opentelemetry/api": "catalog:",
    "@orpc/server": "catalog:",
    "@prisma/adapter-d1": "catalog:",
    "better-auth": "catalog:",
    "elysia": "catalog:",
    "misskey-js": "catalog:",
    "p-retry": "catalog:"
  },
  "scripts": {
    "build": "pnpm type:sync && tsc -p tsconfig.json",
    "dev": "wrangler dev",
    "migrate:local": "wrangler d1 migrations apply midnight-network --local",
    "migrate:remote": "wrangler d1 migrations apply midnight-network --remote",
    "deploy": "wrangler deploy",
    "typecheck": "pnpm type:sync && tsc -p tsconfig.json",
    "test": "pnpm type:sync && vitest run --passWithNoTests",
    "type:sync": "wrangler types"
  },
  "devDependencies": {
    "typescript": "catalog:",
    "vitest": "catalog:",
    "wrangler": "catalog:"
  }
}
