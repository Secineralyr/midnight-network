{
  "name": "@midnight-network/backend",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "dependencies": {
    "@midnight-network/entity": "workspace:*",
    "@midnight-network/rank-calc": "workspace:*",
    "@midnight-network/shared": "workspace:*",
    "@opentelemetry/api": "catalog:",
    "@orpc/server": "catalog:",
    "@prisma/adapter-d1": "catalog:",
    "elysia": "catalog:",
    "misskey-js": "catalog:",
    "p-retry": "catalog:"
  },
  "scripts": {
    "build": "pnpm type:sync && tsc -p tsconfig.json",
    "dev": "pnpm type:sync && wrangler dev",
    "deploy": "pnpm type:sync && wrangler deploy",
    "typecheck": "pnpm type:sync && tsc -p tsconfig.json",
    "test": "pnpm type:sync && vitest run --passWithNoTests",
    "type:sync": "wrangler types"
  },
  "devDependencies": {
    "typescript": "catalog:",
    "vitest": "catalog:",
    "wrangler": "catalog:"
  }
}
